HARBOR will eventually need its own separate document, but not this one. That would be something like:

“Backup Requirements” or “Data Persistence Requirements”

That document would define:

what must be backed up
what is safe to discard

Not how your server does it.